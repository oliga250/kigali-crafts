# API Base URL for the frontend to make requests to the backend.
# If left empty, the API will be relative to the current origin (useful when using a proxy).
# Example: VITE_API_URL=https://example.com
VITE_API_URL=
